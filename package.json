{
  "name": "espn-api-access",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Workspace for the espn-fantasy-client library and its MCP server wrapper.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@12.3.4"
}
